#include "Cake.h"
#include "sub-list.h"
#include <iostream>
using namespace std;

int main() {
  // Creating instances of the Dog class
  Subll<int> wtf;
  wtf.pushBack(10);

  wtf.pushBack(10);
  wtf.pushBack(10);
  wtf.pushBack(10);
  wtf.pushBack(10);
  wtf.pushBack(10);
  cout << "WTF" << endl;
  return 0;
}
